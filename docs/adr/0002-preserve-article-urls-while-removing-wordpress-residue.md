# Preserve Article URLs While Removing WordPress Residue

We will keep existing article URLs such as `/turtle100-1/` while removing WordPress-specific metadata, classes, and asset paths from the Nuxt content source. Preserving URLs protects existing page equity and avoids unnecessary redirects; the Nuxt transition should happen in content structure and asset organization, not by renaming already published pages.
